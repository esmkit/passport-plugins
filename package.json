{
  "private": true,
  "name": "@esmkit/passport-plugins",
  "description": "A set of plugins for passport.",
  "version": "0.3.0",
  "license": "MIT",
  "author": "Billgo <hi@billgo.me>",
  "bugs": "https://github.com/esmkit/passport-plugins/issues",
  "scripts": {
    "start": "bunx kill-port 4000 && bun --hot src/index.ts",
    "publish": "bunx lerna publish --no-git-tag-version --no-push",
    "build": "bunx lerna run build"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@devjskit/copy-files": "^1.1.0",
    "@devjskit/kill-port": "^1.0.0",
    "@devjskit/rimraf": "^1.0.0",
    "@types/bun": "^1.1.14",
    "@types/node": "^22.10.1",
    "@types/web": "^0.0.186",
    "lerna": "^8.1.9",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2"
  }
}